# Closing the lid is the main sleep trigger (no hypridle installed).
# On battery: suspend-then-hibernate so it doesn't drain to 0 and power off.
# On AC: plain suspend (no need to hibernate while plugged in).
[Login]
HandleLidSwitch=suspend-then-hibernate
HandleLidSwitchExternalPower=suspend
