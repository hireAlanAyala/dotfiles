#!/bin/bash
# Persistent clipboard picker - run at startup
# This keeps nvim running for instant clipboard picker access

kitty --class clipboard-picker-daemon -o confirm_os_window_close=0 -e nvim --cmd "let g:clipboard_picker=1" -c "lua require('custom.clipboard-picker').daemon()"
